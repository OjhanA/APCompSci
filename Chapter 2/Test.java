public class Test{
    public static void main (String [] args){
        System.out.println("\f");
            /*
             * int numStudents;
             * numStudents = 34;
             * System.out.println("#1 Number of students: " + numStudents);
             * numStudents = numStudents + 2;
             * System.out.println("#2 Number of students: " + numStudents);
             * double cost = 4.5;
             * double temp = numStudents + cost;
             * numStudents = 30;
             * System.out.println (numStudents + " " + cost + " " + temp);
             */
            
            /*
             * final double TAX_RATE'
             * TAX_RATE = .09;
             * int x;
             * x = 5;
             * int y = 4;
             * System.out.println ("x: " + x + "\ny: " + y);
             */
            
        int x = 6;
        int y = 4;
        int z = x + y;
        int a = x - y;
        int b = x * y;
        double c = (double)x / y;
        int d = x % y;
        int e = y % x;
        System.out.println ("a: " + a + " b: " + b + " c: " + c + " d: " + d + " e: " + e);        
    }
}